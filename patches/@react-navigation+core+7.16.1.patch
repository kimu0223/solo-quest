diff --git a/node_modules/@react-navigation/core/lib/module/StaticNavigation.js b/node_modules/@react-navigation/core/lib/module/StaticNavigation.js
index e20d571..1c8ff1b 100644
--- a/node_modules/@react-navigation/core/lib/module/StaticNavigation.js
+++ b/node_modules/@react-navigation/core/lib/module/StaticNavigation.js
@@ -39,7 +39,10 @@ const MemoizedScreen = /*#__PURE__*/React.memo(({
 });
 MemoizedScreen.displayName = 'Memo(Screen)';
 const getItemsFromScreens = (Screen, screens) => {
-  return Object.entries(screens).map(([name, item]) => {
+  const _screenEntries = Object.entries(screens);
+  const _result = [];
+  for (let _i = 0; _i < _screenEntries.length; _i++) {
+    const [name, item] = _screenEntries[_i];
     let component;
     let props = {};
     let useIf;
@@ -70,18 +73,23 @@ const getItemsFromScreens = (Screen, screens) => {
     const element = isNavigator ? (/*#__PURE__*/React.createElement(component, {})) : /*#__PURE__*/_jsx(MemoizedScreen, {
       component: component
     });
-    return () => {
-      const shouldRender = useIf == null || useIf();
+    const _capturedName = name;
+    const _capturedProps = props;
+    const _capturedUseIf = useIf;
+    const _capturedElement = element;
+    _result.push(() => {
+      const shouldRender = _capturedUseIf == null || _capturedUseIf();
       if (!shouldRender) {
         return null;
       }
       return /*#__PURE__*/_jsx(Screen, {
-        name: name,
-        ...props,
-        children: () => element
-      }, name);
-    };
-  });
+        name: _capturedName,
+        ..._capturedProps,
+        children: () => _capturedElement
+      }, _capturedName);
+    });
+  }
+  return _result;
 };
 
 /**
@@ -115,29 +123,39 @@ export function createComponentForStaticNavigation(tree, displayName) {
       items.push(...getItemsFromScreens(Screen, screens));
     }
     if (key === 'groups' && groups) {
-      items.push(...Object.entries(groups).map(([key, {
-        if: useIf,
-        ...group
-      }]) => {
-        const groupItems = getItemsFromScreens(Screen, group.screens);
-        return () => {
+      const _groupEntries = Object.entries(groups);
+      for (let _gi = 0; _gi < _groupEntries.length; _gi++) {
+        const [_groupKey, _groupVal] = _groupEntries[_gi];
+        const { if: _useIf, ..._group } = _groupVal;
+        const _groupItems = getItemsFromScreens(Screen, _group.screens);
+        const _capturedGroupKey = _groupKey;
+        const _capturedGroup = _group;
+        const _capturedUseIf = _useIf;
+        const _capturedGroupItems = _groupItems;
+        items.push(() => {
           // Call unconditionally since screen configs may contain `useIf` hooks
-          const children = groupItems.map(item => item());
-          const shouldRender = useIf == null || useIf();
+          const children = [];
+          for (let _ci = 0; _ci < _capturedGroupItems.length; _ci++) {
+            children.push(_capturedGroupItems[_ci]());
+          }
+          const shouldRender = _capturedUseIf == null || _capturedUseIf();
           if (!shouldRender) {
             return null;
           }
           return /*#__PURE__*/_jsx(Group, {
-            navigationKey: key,
-            ...group,
+            navigationKey: _capturedGroupKey,
+            ..._capturedGroup,
             children: children
-          }, key);
-        };
-      }));
+          }, _capturedGroupKey);
+        });
+      }
     }
   }
   const NavigatorComponent = () => {
-    const children = items.map(item => item());
+    const children = [];
+    for (let _ii = 0; _ii < items.length; _ii++) {
+      children.push(items[_ii]());
+    }
     return /*#__PURE__*/_jsx(Navigator, {
       ...rest,
       children: children
@@ -170,18 +188,17 @@ export function createPathConfigForStaticNavigation(tree, options, auto) {
   let initialScreenConfig;
   const createPathConfigForTree = (t, o, skipInitialDetection) => {
     const createPathConfigForScreens = (screens, initialRouteName) => {
-      return Object.fromEntries(Object.entries(screens)
       // Re-order to move the initial route to the front
       // This way we can detect the initial route correctly
-      .sort(([a], [b]) => {
-        if (a === initialRouteName) {
-          return -1;
-        }
-        if (b === initialRouteName) {
-          return 1;
-        }
+      const _screenEntries = Object.entries(screens);
+      _screenEntries.sort(([a], [b]) => {
+        if (a === initialRouteName) { return -1; }
+        if (b === initialRouteName) { return 1; }
         return 0;
-      }).map(([key, item]) => {
+      });
+      const _result = {};
+      for (let _i = 0; _i < _screenEntries.length; _i++) {
+        const [key, item] = _screenEntries[_i];
         const screenConfig = {};
         if ('linking' in item) {
           if (typeof item.linking === 'string') {
@@ -194,15 +211,15 @@ export function createPathConfigForStaticNavigation(tree, options, auto) {
             .replace(/\/$/, ''); // Remove extra trailing slash
           }
         }
-        let screens;
+        let childScreens;
         const skipInitialDetectionInChild = skipInitialDetection || screenConfig.path != null && screenConfig.path !== '';
         if ('config' in item) {
-          screens = createPathConfigForTree(item, undefined, skipInitialDetectionInChild);
+          childScreens = createPathConfigForTree(item, undefined, skipInitialDetectionInChild);
         } else if ('screen' in item && 'config' in item.screen && (item.screen.config.screens || item.screen.config.groups)) {
-          screens = createPathConfigForTree(item.screen, undefined, skipInitialDetectionInChild);
+          childScreens = createPathConfigForTree(item.screen, undefined, skipInitialDetectionInChild);
         }
-        if (screens) {
-          screenConfig.screens = screens;
+        if (childScreens) {
+          screenConfig.screens = childScreens;
         }
         if (auto && !screenConfig.screens &&
         // Skip generating path for screens that specify linking config as `undefined` or `null` explicitly
@@ -224,8 +241,11 @@ export function createPathConfigForStaticNavigation(tree, options, auto) {
             screenConfig.path = key.replace(/([A-Z]+)/g, '-$1').replace(/^-/, '').toLowerCase();
           }
         }
-        return [key, screenConfig];
-      }).filter(([, screen]) => Object.keys(screen).length > 0));
+        if (Object.keys(screenConfig).length > 0) {
+          _result[key] = screenConfig;
+        }
+      }
+      return _result;
     };
     const screens = {};
 
@@ -236,9 +256,11 @@ export function createPathConfigForStaticNavigation(tree, options, auto) {
         Object.assign(screens, createPathConfigForScreens(t.config.screens, o?.initialRouteName ?? t.config.initialRouteName));
       }
       if (key === 'groups' && t.config.groups) {
-        Object.entries(t.config.groups).forEach(([, group]) => {
-          Object.assign(screens, createPathConfigForScreens(group.screens, o?.initialRouteName ?? t.config.initialRouteName));
-        });
+        const _tGroupEntries = Object.entries(t.config.groups);
+        for (let _tgi = 0; _tgi < _tGroupEntries.length; _tgi++) {
+          const _tGroup = _tGroupEntries[_tgi][1];
+          Object.assign(screens, createPathConfigForScreens(_tGroup.screens, o?.initialRouteName ?? t.config.initialRouteName));
+        }
       }
     }
     if (Object.keys(screens).length === 0) {
diff --git a/node_modules/@react-navigation/core/lib/module/getPathFromState.js b/node_modules/@react-navigation/core/lib/module/getPathFromState.js
index ef4e3af..4edabcb 100644
--- a/node_modules/@react-navigation/core/lib/module/getPathFromState.js
+++ b/node_modules/@react-navigation/core/lib/module/getPathFromState.js
@@ -76,20 +76,25 @@ export function getPathFromState(state, options) {
       nestedRouteNames.push(route.name);
       if (route.params) {
         const options = currentOptions[route.name];
-        const currentParams = Object.fromEntries(Object.entries(route.params).map(([key, value]) => {
+        // Use for loop instead of .map() to avoid Hermes GC bug with String() calls
+        const currentParamsEntries = [];
+        const routeParamEntries = Object.entries(route.params);
+        for (let pi = 0; pi < routeParamEntries.length; pi++) {
+          const [key, value] = routeParamEntries[pi];
           if (value === undefined) {
             if (options) {
               const optional = options.parts?.find(part => part.param === key)?.optional;
               if (optional) {
-                return null;
+                continue;
               }
             } else {
-              return null;
+              continue;
             }
           }
           const stringify = options?.stringify?.[key] ?? String;
-          return [key, stringify(value)];
-        }).filter(entry => entry != null));
+          currentParamsEntries.push([key, stringify(value)]);
+        }
+        const currentParams = Object.fromEntries(currentParamsEntries);
         if (parts?.length) {
           Object.assign(allParams, currentParams);
         }
@@ -134,37 +139,56 @@ export function getPathFromState(state, options) {
       }
     }
     if (currentOptions[route.name] !== undefined) {
-      path += parts?.map(({
-        segment,
-        param,
-        optional
-      }) => {
-        // We don't know what to show for wildcard patterns
-        // Showing the route name seems ok, though whatever we show here will be incorrect
-        // Since the page doesn't actually exist
-        if (segment === '*') {
-          return route.name;
-        }
-
-        // If the path has a pattern for a param, put the param in the path
-        if (param) {
-          const value = allParams[param];
-          if (value === undefined && optional) {
-            // Optional params without value assigned in route.params should be ignored
-            return '';
+      // Use for loop instead of .map() to avoid Hermes GC bug (EXC_BAD_ACCESS)
+      // when creating strings inside arrayPrototypeMap
+      if (parts) {
+        const pathSegments = [];
+        for (let si = 0; si < parts.length; si++) {
+          const { segment, param, optional } = parts[si];
+          // We don't know what to show for wildcard patterns
+          // Showing the route name seems ok, though whatever we show here will be incorrect
+          // Since the page doesn't actually exist
+          if (segment === '*') {
+            pathSegments.push(route.name);
+            continue;
           }
 
-          // Valid characters according to
-          // https://datatracker.ietf.org/doc/html/rfc3986#section-3.3 (see pchar definition)
-          return Array.from(String(value)).map(char => /[^A-Za-z0-9\-._~!$&'()*+,;=:@]/g.test(char) ? encodeURIComponent(char) : char).join('');
+          // If the path has a pattern for a param, put the param in the path
+          if (param) {
+            const value = allParams[param];
+            if (value === undefined && optional) {
+              // Optional params without value assigned in route.params should be ignored
+              pathSegments.push('');
+              continue;
+            }
+
+            // Valid characters according to
+            // https://datatracker.ietf.org/doc/html/rfc3986#section-3.3 (see pchar definition)
+            const strValue = String(value);
+            let encodedValue = '';
+            for (let ci = 0; ci < strValue.length; ci++) {
+              const char = strValue[ci];
+              encodedValue += /[^A-Za-z0-9\-._~!$&'()*+,;=:@]/g.test(char) ? encodeURIComponent(char) : char;
+            }
+            pathSegments.push(encodedValue);
+            continue;
+          }
+          pathSegments.push(encodeURIComponent(segment));
         }
-        return encodeURIComponent(segment);
-      }).join('/');
+        path += pathSegments.join('/');
+      }
     } else {
       path += encodeURIComponent(route.name);
     }
     if (!focusedParams && focusedRoute.params) {
-      focusedParams = Object.fromEntries(Object.entries(focusedRoute.params).map(([key, value]) => [key, String(value)]));
+      // Use for loop instead of .map() to avoid Hermes GC bug with String() calls
+      const focusedParamsEntries = [];
+      const focusedRouteParamEntries = Object.entries(focusedRoute.params);
+      for (let fi = 0; fi < focusedRouteParamEntries.length; fi++) {
+        const [key, value] = focusedRouteParamEntries[fi];
+        focusedParamsEntries.push([key, String(value)]);
+      }
+      focusedParams = Object.fromEntries(focusedParamsEntries);
     }
     if (route.state) {
       path += '/';
diff --git a/node_modules/@react-navigation/core/lib/module/getPatternParts.js b/node_modules/@react-navigation/core/lib/module/getPatternParts.js
index 59b8008..37cc85c 100644
--- a/node_modules/@react-navigation/core/lib/module/getPatternParts.js
+++ b/node_modules/@react-navigation/core/lib/module/getPatternParts.js
@@ -90,7 +90,10 @@ export function getPatternParts(path) {
   if (isRegex) {
     throw new Error(`Could not find closing ')' in path: ${path}`);
   }
-  const params = parts.map(part => part.param).filter(Boolean);
+  const params = [];
+  for (let _i = 0; _i < parts.length; _i++) {
+    if (parts[_i].param) params.push(parts[_i].param);
+  }
   for (const [index, param] of params.entries()) {
     if (params.indexOf(param) !== index) {
       throw new Error(`Duplicate param name '${param}' found in path: ${path}`);
diff --git a/node_modules/@react-navigation/core/lib/module/getStateFromPath.js b/node_modules/@react-navigation/core/lib/module/getStateFromPath.js
index 7132844..db67fa5 100644
--- a/node_modules/@react-navigation/core/lib/module/getStateFromPath.js
+++ b/node_modules/@react-navigation/core/lib/module/getStateFromPath.js
@@ -137,7 +137,17 @@ function getInitialRoutes(options) {
 }
 function getSortedNormalizedConfigs(initialRoutes, screens = {}) {
   // Create a normalized configs array which will be easier to use
-  return [].concat(...Object.keys(screens).map(key => createNormalizedConfigs(key, screens, initialRoutes, [], [], []))).sort((a, b) => {
+  // Use for loop instead of .map() + concat to avoid Hermes GC bug (EXC_BAD_ACCESS)
+  // when creating objects inside arrayPrototypeMap with nested forEach calls
+  const allConfigs = [];
+  const screenKeys = Object.keys(screens);
+  for (let i = 0; i < screenKeys.length; i++) {
+    const result = createNormalizedConfigs(screenKeys[i], screens, initialRoutes, [], [], []);
+    for (let j = 0; j < result.length; j++) {
+      allConfigs.push(result[j]);
+    }
+  }
+  return allConfigs.sort((a, b) => {
     // Sort config from most specific to least specific:
     // - more segments
     // - static segments
@@ -235,11 +245,19 @@ function checkForDuplicatedConfigs(configs) {
   }, {});
 }
 function getConfigsWithRegexes(configs) {
-  return configs.map(c => ({
-    ...c,
-    // Add `$` to the regex to make sure it matches till end of the path and not just beginning
-    regex: c.regex ? new RegExp(c.regex.source + '$') : undefined
-  }));
+  // Use a for loop instead of .map() to avoid a Hermes GC bug (EXC_BAD_ACCESS crash)
+  // where creating new RegExp() inside arrayPrototypeMap corrupts DictPropertyMap.
+  // See: Hermes crash in DictPropertyMap::findOrAdd during JSRegExp::initialize
+  const result = [];
+  for (let i = 0; i < configs.length; i++) {
+    const c = configs[i];
+    result.push({
+      ...c,
+      // Add `$` to the regex to make sure it matches till end of the path and not just beginning
+      regex: c.regex ? new RegExp(c.regex.source + '$') : undefined
+    });
+  }
+  return result;
 }
 const matchAgainstConfigs = (remaining, configs) => {
   let routes;
@@ -254,36 +272,48 @@ const matchAgainstConfigs = (remaining, configs) => {
 
     // If our regex matches, we need to extract params from the path
     if (match) {
-      routes = config.routeNames.map(routeName => {
+      // Use for loops instead of .map() to avoid Hermes GC bug (EXC_BAD_ACCESS)
+      // when creating objects inside Array.prototype.map()
+      routes = [];
+      for (let ri = 0; ri < config.routeNames.length; ri++) {
+        const routeName = config.routeNames[ri];
         const routeConfig = configs.find(c => {
           // Check matching name AND pattern in case same screen is used at different levels in config
           return c.screen === routeName && arrayStartsWith(config.segments, c.segments);
         });
-        const params = routeConfig && match.groups ? Object.fromEntries(Object.entries(match.groups).map(([key, value]) => {
-          const index = Number(key.replace('param_', ''));
-          const param = routeConfig.params.find(it => it.index === index);
-          if (param?.screen === routeName && param?.name) {
-            return [param.name, value];
+        let params = undefined;
+        if (routeConfig && match.groups) {
+          const groupEntries = Object.entries(match.groups);
+          const rawParams = [];
+          for (let gi = 0; gi < groupEntries.length; gi++) {
+            const [key, value] = groupEntries[gi];
+            const index = Number(key.replace('param_', ''));
+            const param = routeConfig.params.find(it => it.index === index);
+            if (param?.screen === routeName && param?.name) {
+              rawParams.push([param.name, value]);
+            }
+          }
+          const parsedParams = [];
+          for (let pi = 0; pi < rawParams.length; pi++) {
+            const [key, value] = rawParams[pi];
+            if (value == null) {
+              parsedParams.push([key, undefined]);
+            } else {
+              const decoded = decodeURIComponent(value);
+              const parsed = routeConfig.parse?.[key] ? routeConfig.parse[key](decoded) : decoded;
+              parsedParams.push([key, parsed]);
+            }
           }
-          return null;
-        }).filter(it => it != null).map(([key, value]) => {
-          if (value == null) {
-            return [key, undefined];
+          if (parsedParams.length) {
+            params = Object.fromEntries(parsedParams);
           }
-          const decoded = decodeURIComponent(value);
-          const parsed = routeConfig.parse?.[key] ? routeConfig.parse[key](decoded) : decoded;
-          return [key, parsed];
-        })) : undefined;
+        }
         if (params && Object.keys(params).length) {
-          return {
-            name: routeName,
-            params
-          };
+          routes.push({ name: routeName, params });
+        } else {
+          routes.push({ name: routeName });
         }
-        return {
-          name: routeName
-        };
-      });
+      }
       remainingPath = remainingPath.replace(match[0], '');
       break;
     }
@@ -370,28 +400,41 @@ const createConfigItem = (screen, routeNames, paths, parse) => {
   const parts = [];
 
   // Parse the path string into parts for easier matching
+  // Use for loop instead of .map() to avoid Hermes GC bug (EXC_BAD_ACCESS)
+  // when creating objects with spread inside arrayPrototypeMap
   for (const {
     screen,
     path
   } of paths) {
-    parts.push(...getPatternParts(path).map(part => ({
-      ...part,
-      screen
-    })));
+    const patternParts = getPatternParts(path);
+    for (let ppi = 0; ppi < patternParts.length; ppi++) {
+      parts.push({ ...patternParts[ppi], screen });
+    }
   }
-  const regex = parts.length ? new RegExp(`^(${parts.map((it, i) => {
+  // Use for loops instead of .map() to avoid Hermes GC bug (EXC_BAD_ACCESS)
+  // when creating new RegExp() inside Array.prototype.map()
+  let regexParts = '';
+  for (let i = 0; i < parts.length; i++) {
+    const it = parts[i];
     if (it.param) {
       const reg = it.regex || '[^/]+';
-      return `(((?<param_${i}>${reg})\\/)${it.optional ? '?' : ''})`;
+      regexParts += `(((?<param_${i}>${reg})\\/)${it.optional ? '?' : ''})`;
+    } else {
+      regexParts += `${it.segment === '*' ? '.*' : escape(it.segment)}\\/`;
+    }
+  }
+  const regex = parts.length ? new RegExp(`^(${regexParts})$`) : undefined;
+  const segments = [];
+  for (let i = 0; i < parts.length; i++) {
+    segments.push(parts[i].segment);
+  }
+  const params = [];
+  for (let i = 0; i < parts.length; i++) {
+    const it = parts[i];
+    if (it.param) {
+      params.push({ index: i, screen: it.screen, name: it.param });
     }
-    return `${it.segment === '*' ? '.*' : escape(it.segment)}\\/`;
-  }).join('')})$`) : undefined;
-  const segments = parts.map(it => it.segment);
-  const params = parts.map((it, i) => it.param ? {
-    index: i,
-    screen: it.screen,
-    name: it.param
-  } : null).filter(it => it != null);
+  }
   return {
     screen,
     regex,
diff --git a/node_modules/@react-navigation/core/lib/module/useEventEmitter.js b/node_modules/@react-navigation/core/lib/module/useEventEmitter.js
index 3a68aad..7ea70b9 100644
--- a/node_modules/@react-navigation/core/lib/module/useEventEmitter.js
+++ b/node_modules/@react-navigation/core/lib/module/useEventEmitter.js
@@ -48,7 +48,27 @@ export function useEventEmitter(listen) {
     const items = listeners.current[type] || {};
 
     // Copy the current list of callbacks in case they are mutated during execution
-    const callbacks = target !== undefined ? items[target]?.slice() : [].concat(...Object.keys(items).map(t => items[t])).filter((cb, i, self) => self.lastIndexOf(cb) === i);
+    let callbacks;
+    if (target !== undefined) {
+      callbacks = items[target]?.slice();
+    } else {
+      const _itemKeys = Object.keys(items);
+      const _allCbs = [];
+      for (let _i = 0; _i < _itemKeys.length; _i++) {
+        const _group = items[_itemKeys[_i]];
+        if (_group) {
+          for (let _j = 0; _j < _group.length; _j++) {
+            _allCbs.push(_group[_j]);
+          }
+        }
+      }
+      callbacks = [];
+      for (let _i = 0; _i < _allCbs.length; _i++) {
+        if (_allCbs[_i] && _allCbs.lastIndexOf(_allCbs[_i]) === _i) {
+          callbacks.push(_allCbs[_i]);
+        }
+      }
+    }
     const event = {
       get type() {
         return type;
diff --git a/node_modules/@react-navigation/core/lib/module/useNavigationBuilder.js b/node_modules/@react-navigation/core/lib/module/useNavigationBuilder.js
index cd5e325..8a77945 100644
--- a/node_modules/@react-navigation/core/lib/module/useNavigationBuilder.js
+++ b/node_modules/@react-navigation/core/lib/module/useNavigationBuilder.js
@@ -195,7 +195,10 @@ export function useNavigationBuilder(createRouter, options) {
     acc[config.props.name] = config;
     return acc;
   }, {});
-  const routeNames = routeConfigs.map(config => config.props.name);
+  const routeNames = [];
+  for (let _i = 0; _i < routeConfigs.length; _i++) {
+    routeNames.push(routeConfigs[_i].props.name);
+  }
   const routeKeyList = routeNames.reduce((acc, curr) => {
     acc[curr] = screens[curr].keys.map(key => key ?? '').join(':');
     return acc;
@@ -467,24 +470,34 @@ export function useNavigationBuilder(createRouter, options) {
       return;
     }
     const navigation = descriptors[route.key].navigation;
-    const listeners = [].concat(
-    // Get an array of listeners for all screens + common listeners on navigator
-    ...[screenListeners, ...routeNames.map(name => {
-      const {
-        listeners
-      } = screens[name].props;
-      return listeners;
-    })].map(listeners => {
-      const map = typeof listeners === 'function' ? listeners({
-        route: route,
-        navigation
-      }) : listeners;
-      return map ? Object.keys(map).filter(type => type === e.type).map(type => map?.[type]) : undefined;
-    }))
-    // We don't want same listener to be called multiple times for same event
-    // So we remove any duplicate functions from the array
-    .filter((cb, i, self) => cb && self.lastIndexOf(cb) === i);
-    listeners.forEach(listener => listener?.(e));
+    // Step1: collect listener sources
+    const _allListenerSources = [screenListeners];
+    for (let _li = 0; _li < routeNames.length; _li++) {
+      _allListenerSources.push(screens[routeNames[_li]].props.listeners);
+    }
+    // Step2: evaluate and collect callbacks
+    const _rawCallbacks = [];
+    for (let _si = 0; _si < _allListenerSources.length; _si++) {
+      const _listenerSource = _allListenerSources[_si];
+      const _map = typeof _listenerSource === 'function'
+        ? _listenerSource({ route: route, navigation })
+        : _listenerSource;
+      if (!_map) continue;
+      const _mapKeys = Object.keys(_map);
+      for (let _ki = 0; _ki < _mapKeys.length; _ki++) {
+        if (_mapKeys[_ki] === e.type) _rawCallbacks.push(_map[_mapKeys[_ki]]);
+      }
+    }
+    // Step3: deduplicate
+    const listeners = [];
+    for (let _ci = 0; _ci < _rawCallbacks.length; _ci++) {
+      const _cb = _rawCallbacks[_ci];
+      if (_cb && _rawCallbacks.lastIndexOf(_cb) === _ci) listeners.push(_cb);
+    }
+    // Step4: execute
+    for (let _fi = 0; _fi < listeners.length; _fi++) {
+      listeners[_fi]?.(e);
+    }
   });
   useFocusEvents({
     state,
diff --git a/node_modules/@react-navigation/core/lib/module/useOnGetState.js b/node_modules/@react-navigation/core/lib/module/useOnGetState.js
index 4218f27..a904f5a 100644
--- a/node_modules/@react-navigation/core/lib/module/useOnGetState.js
+++ b/node_modules/@react-navigation/core/lib/module/useOnGetState.js
@@ -17,16 +17,12 @@ export function useOnGetState({
     const state = getState();
 
     // Avoid returning new route objects if we don't need to
-    const routes = state.routes.map(route => {
+    const routes = [];
+    for (let _i = 0; _i < state.routes.length; _i++) {
+      const route = state.routes[_i];
       const childState = getStateListeners[route.key]?.();
-      if (route.state === childState) {
-        return route;
-      }
-      return {
-        ...route,
-        state: childState
-      };
-    });
+      routes.push(route.state === childState ? route : { ...route, state: childState });
+    }
     if (isArrayEqual(state.routes, routes)) {
       return state;
     }
diff --git a/node_modules/@react-navigation/core/lib/module/validatePathConfig.js b/node_modules/@react-navigation/core/lib/module/validatePathConfig.js
index 1af1990..3065151 100644
--- a/node_modules/@react-navigation/core/lib/module/validatePathConfig.js
+++ b/node_modules/@react-navigation/core/lib/module/validatePathConfig.js
@@ -16,7 +16,10 @@ export function validatePathConfig(config, root = true) {
   if (typeof config !== 'object' || config === null) {
     throw new Error(`Expected the configuration to be an object, but got ${JSON.stringify(config)}.`);
   }
-  const validationErrors = Object.fromEntries(Object.keys(config).map(key => {
+  const validationErrors = {};
+  const _configKeys = Object.keys(config);
+  for (let _i = 0; _i < _configKeys.length; _i++) {
+    const key = _configKeys[_i];
     if (key in validation) {
       const type = validation[key];
       // @ts-expect-error: we know the key exists
@@ -24,17 +27,16 @@ export function validatePathConfig(config, root = true) {
       if (value !== undefined) {
         if (type === 'array') {
           if (!Array.isArray(value)) {
-            return [key, `expected 'Array', got '${typeof value}'`];
+            validationErrors[key] = `expected 'Array', got '${typeof value}'`;
           }
         } else if (typeof value !== type) {
-          return [key, `expected '${type}', got '${typeof value}'`];
+          validationErrors[key] = `expected '${type}', got '${typeof value}'`;
         }
       }
     } else {
-      return [key, 'extraneous'];
+      validationErrors[key] = 'extraneous';
     }
-    return null;
-  }).filter(Boolean));
+  }
   if (Object.keys(validationErrors).length) {
     throw new Error(`Found invalid properties in the configuration:\n${formatToList(validationErrors)}\n\nYou can only specify the following properties:\n${formatToList(validation)}\n\nIf you want to specify configuration for screens, you need to specify them under a 'screens' property.\n\nSee https://reactnavigation.org/docs/configuring-links for more details on how to specify a linking configuration.`);
   }
